{
  "name": "cocktails",
  "version": "1.0.0",
  "description": "Determine what cocktails you can make based on the ingredients in your bar",
  "main": "index.js",
  "scripts": {
    "deploy": "npm run deploy:graphql",
    "deploy:graphql": "cd graphql && serverless deploy",
    "install": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "WTFPL",
  "devDependencies": {
    "@types/node": "^8.0.24",
    "serverless": "^1.20.2",
    "typescript": "^2.4.2"
  }
}
